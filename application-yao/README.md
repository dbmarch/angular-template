Can build only for linux

Can't seem to specify the other architectures

sudo apt install arch-test
arch-test
amd64
i386
win32
win64
x32

sudo apt install qemu-user qemu-user-binfmt

After installing I can build everything
